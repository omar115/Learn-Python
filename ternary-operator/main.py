"""
ternary operator is writing python code in a
smarter way. Instead of writing if else block
code, we can define all these things in one line.
the message variable section I introduce how we can
use ternary operator.
"""

age = 22

message = "Eligible" if age >= 18 else "Not Eligible"

print(message)